(l: List) each: (b: Block) := {
    l map: b in-scope
    l
} call

[] includes?: List := False
(x: List) includes?: (y: List) :=
    if: (x (take: y length) == y)
        then: { True }
        else: { x tail includes?: y }
